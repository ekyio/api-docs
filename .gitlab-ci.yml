pages:
  image: node:14
  stage: deploy
  script:
    - npm i -g insomnia-documenter
    - insomnia-documenter --config spec.json --output public/
  artifacts:
    paths:
      - public
  only:
  - master
